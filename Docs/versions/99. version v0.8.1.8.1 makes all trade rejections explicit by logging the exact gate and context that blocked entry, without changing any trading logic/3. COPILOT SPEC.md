COPILOT SPEC (FINAL — LOCKED)
Midas_V2 v0.8.1.8.2
Feature: DAY_GATE Day-Level Summary Logging

====================================================================
GOAL (EXACT)
====================================================================

Add a single, explicit, day-level diagnostic log that summarizes the
DAY_GATE outcome for each trading day and scenario.

This version is LOGGING ONLY.
It MUST NOT change any trading behavior, thresholds, or outcomes.

====================================================================
BACKGROUND (DO NOT RE-INTERPRET)
====================================================================

From v0.8.1.8.1 validation across April, July, and August 2025:

- The confirm-bar stop guard works correctly.
- DAY_GATE is the dominant suppressor of trades and profit.
- DAY_GATE failures are currently visible only via many per-symbol logs.
- We need a single day-level summary to explain 0-trade days immediately.

This version completes the diagnostic phase before any profit recovery work.

====================================================================
FILES ALLOWED TO CHANGE (ONLY)
====================================================================

- src/midas_v2/engine/backtester.py

NO OTHER FILES MAY BE MODIFIED.

====================================================================
ABSOLUTE RULES (NON-NEGOTIABLE)
====================================================================

- DO NOT change strategy logic.
- DO NOT change DAY_GATE behavior.
- DO NOT change thresholds or config.
- DO NOT add new indicators or computations.
- DO NOT add new helper modules.
- DO NOT add new config keys.
- DO NOT affect trade count, entries, exits, or PnL.

If trade results change, the implementation is WRONG.

====================================================================
REQUIRED OUTPUT (EXACT)
====================================================================

For EACH trading day and EACH scenario, emit EXACTLY ONE log line
after DAY_GATE evaluation is fully finalized.

The log MUST appear whether the day PASSES or FAILS DAY_GATE.

FORMAT (MUST MATCH — SINGLE LINE):

[WHY] v0.8.1.8.2 DAY_GATE_SUMMARY details=enabled=<bool> gate_minutes=<N> min_symbols=<N> close_gt_vwap_cnt=<N> require_close_gt_vwap=<bool> day_gate_failed=<bool>

====================================================================
DETAILS FIELD REQUIREMENTS
====================================================================

- Use ONLY existing variables already computed in v0.8.1.8.1
- Do NOT compute new counters
- Do NOT derive new metrics
- Values MUST reflect the FINAL day-level state actually used downstream

Expected variables (use the exact names you already have in scope):
- require_day_follow_through
- day_follow_through_minutes
- day_follow_through_min_symbols
- close_gt_vwap_count
- require_day_gate_close_gt_vwap
- day_gate_failed

====================================================================
INSERTION POINT (CRITICAL)
====================================================================

Insert the DAY_GATE_SUMMARY log at THIS EXACT POINT:

- AFTER:
  - DAY_GATE evaluation is complete, AND
  - close_gt_vwap_count is finalized, AND
  - the structural-damage auto-switch is finalized
    (i.e., after the existing log:
     "STRUCT_DAMAGE v0.8.1.5.0: CONFIG base=... effective=...")

- BEFORE:
  - any per-symbol trading loop begins
  - any initialization of trades_by_symbol or similar structures

The summary MUST:
- execute once per day
- execute once per scenario
- NOT be inside a per-symbol loop
- NOT be inside a per-bar loop

If the log prints more than once per day, the implementation is incorrect.

====================================================================
LOGGING LEVEL
====================================================================

- Use INFO level (preferred) or WARNING
- MUST include the exact prefix:
  "[WHY] v0.8.1.8.2 DAY_GATE_SUMMARY"

====================================================================
EXAMPLES (FORMAT ONLY — VALUES WILL DIFFER)
====================================================================

[WHY] v0.8.1.8.2 DAY_GATE_SUMMARY details=enabled=True gate_minutes=20 min_symbols=2 close_gt_vwap_cnt=0 require_close_gt_vwap=True day_gate_failed=True

[WHY] v0.8.1.8.2 DAY_GATE_SUMMARY details=enabled=True gate_minutes=20 min_symbols=2 close_gt_vwap_cnt=3 require_close_gt_vwap=True day_gate_failed=False

====================================================================
VALIDATION (MANDATORY)
====================================================================

After implementation, run:

python scripts\run_range_and_summarize.py --start 2025-07-14 --end 2025-07-18 --scenario B

Expected:
- Trade counts and PnL are IDENTICAL to v0.8.1.8.1
- Exactly one DAY_GATE_SUMMARY line per trading day
- Summary explains why days produced 0 trades

Also run a known good period:

python scripts\run_range_and_summarize.py --start 2025-08-05 --end 2025-08-09 --scenario B

Expected:
- DAY_GATE_SUMMARY appears for each day
- Trades remain present
- No regressions

====================================================================
DELIVERABLE
====================================================================

- One small commit
- backtester.py only
- Logging-only change
- No behavior changes

END SPEC
