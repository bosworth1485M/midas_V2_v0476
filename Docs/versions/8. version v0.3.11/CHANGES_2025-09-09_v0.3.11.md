Version: v0.3.11

Summary:
- Project guard added (`scripts/preflight_project_guard.py`) to avoid confusion with the old TOML project.
- Reconfirmed **JSON** config (config/scenarios.json) as the single source of truth.
- Holiday/weekend handling prints INFO and skips cleanly; Polygon auth stable (Bearer + sanitized key).
- September D/E (1→9) smoke produced 0 trades (strict guards); August D baseline remains WR 63.29%, +167.57 PnL.

What led to this version:
- Temporary confusion over TOML vs JSON (from the abandoned project). Resolved by:
  - Implementing **preflight** to print the live JSON config and D/E keys before runs.
  - Running `Docs\refresh_docs.py --root .` to keep SCRIPTS_INDEX and guides in sync.
- Verified non-trading-day handling across 2025-09-01/06/07; graceful skips without failures.
- Regression smoke (`scripts\regression_smoke_v1.py`) streams output; confirmed August D and September pipeline stability.

How to preflight & run:
```
python scripts\preflight_project_guard.py
python Docs\refresh_docs.py --root .
python scripts\run_day_simple.py --date 2025-09-02 --scenario D
python scripts\run_range_and_summarize.py --start 2025-09-01 --end 2025-09-09 --scenario D
python scripts\analyze_range_explained.py --csv out\auto\range_summary_20250901_20250909_D.csv
```
Next:
- Measure July D/E; if needed, A/B hygiene and opening RVOL gate to lift WR and PnL.
