Here are the exact single-line commands to validate v0.8.1.7.1 (execution correctness hotfix) after Copilot applies the patch.

1) Fast sanity check (the “impossible day”)

Re-run 2025-04-07 only (Scenario B) and save the log:

python scripts\run_range_and_summarize.py --start 2025-04-07 --end 2025-04-07 --scenario B | Tee-Object -FilePath .\out\20250407\B\runlog_2025-04-07_B_v0.8.1.7.1.txt


Then extract the key totals + any new WHY lines:

Select-String -Path .\out\20250407\B\runlog_2025-04-07_B_v0.8.1.7.1.txt -Pattern "TP_SL_REBASE|OUTCOME_PNL_MISMATCH|TP_but_negative_pnl|TP=|SL=|Win%|totalPnL|pnl="


Pass condition: you should no longer see “Win%=100%” paired with negative totalPnL due to TP trades.

2) Re-run the known-bad cluster (single-day loop, one line)

This runs each known-bad day one-by-one using your range runner:

python -c "import subprocess; days=['2025-04-01','2025-04-07','2025-08-20','2025-08-29','2025-09-10']; [subprocess.run(['python','scripts/run_range_and_summarize.py','--start',d,'--end',d,'--scenario','B'],check=False) for d in days]"

3) Re-run the invariant scanner (must go to zero for outcome==TP)

This must return 0 for “TP but negative pnl”:

python -c "import glob,csv; bad=[]; [bad.append((f,r.get('symbol'),r.get('pnl'),r.get('outcome'))) for f in glob.glob('out/2025*/B/results_2025-*.csv') for r in csv.DictReader(open(f,newline='')) if r.get('outcome')=='TP' and float(r.get('pnl') or 0)<0]; print('TP_but_negative_pnl=',len(bad)); print('\n'.join(map(str,bad[:50])))"


Pass condition:
TP_but_negative_pnl= 0

If any negatives still exist, they must show up as your new explicit outcomes:

ERR_TP_NEG_PNL

ERR_SL_POS_PNL

4) Optional: show any ERR outcomes (to confirm the safety net is working)
python -c "import glob,csv; errs=[]; [errs.append((f,r.get('symbol'),r.get('outcome'),r.get('pnl'))) for f in glob.glob('out/2025*/B/results_2025-*.csv') for r in csv.DictReader(open(f,newline='')) if (r.get('outcome') or '').startswith('ERR_')]; print('ERR_outcomes=',len(errs)); print('\n'.join(map(str,errs[:50])))"

What to paste back to me

After you run #1 and #3, paste:

the totals line for 2025-04-07, and

the TP_but_negative_pnl= output

Then I’ll tell you if the hotfix is validated and ready to tag, and we’ll proceed back to v0.8.1.8.0.