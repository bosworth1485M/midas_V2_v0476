Absolutely — here is the corrected, clean, official handover document for your next version, clearly labeled at the top as:

v0.8.1.0.0 — TWCS (Trade-Window Candle Snapshot System), Phase 1

This document is designed to be pasted at the top of your next ChatGPT thread so the new session fully understands the project state, workflow rules, and what this next version is about.

No future instructions are included here — ONLY the handover.

MIDAS_V2 — VERSION v0.8.1.0.0 (Starting Point)
Handover Document from v0.7.9.7.6
1. CURRENT PROJECT CONTEXT

You are developing Midas_V2, a Cameron-style small-cap momentum trading engine with:

A unified Python backtester

A strict JSON-driven configuration system

A local React website (Claude-generated) for scenario control

A Copilot-only coding workflow

Full versioned documentation and reproducibility

The overarching aim is to replicate and surpass the most profitable Cameron-style automated systems by introducing:

Candle-aware entry validation

Microstructure analysis

Catalyst-aware filtering

Risk-aware sizing

TWCS (candle snapshots)

Relational database

Advanced candle pattern analysis

2. DEVELOPMENT WORKFLOW (PERMANENT RULES)

The following rules are strict and stored in system memory:

1. All Python & JSON/config updates must be generated by Copilot

You never manually modify:

Python files

Config files

JSON files

Pydantic models

Database schema files

2. All frontend work must be generated by Claude

All TSX/React components are Claude-only.

3. All changes must be traceable, reproducible, and version-tagged

Every Copilot edit must include inline comments:

# vX.Y.Z: <change description>

4. You always run tests after Copilot edits

Using:

python scripts/run_range_and_summarize.py --start <date> --end <date> --scenario B

5. JSON is the single source of truth

CLI overrides only apply when explicitly set.

3. WHAT WAS COMPLETED IN v0.7.9.7.6
3.1 Scanner → JSON (Step 1)

Scenario B now fully defines:

min_price

max_price

min_gap_pct

top

topgappers.py resolves scanner parameters using:

Scenario JSON

CLI override (explicit only)

Error fallback only if misconfigured

3.2 Strategy → JSON (Step 2)

StrategyParams are now constructed from Scenario B JSON defaults via:

create_strategy_params(scenario_name, **norm_params)


This includes:

Gate minutes

PM volume

RVOL gate

Price pattern gate (green streak)

MACD histogram gate

TP/SL percentages

Dip-reclaim flag

Strategy logs match the Scenario B Atlas exactly.

3.3 Risk → JSON + Diagnostic System (Step 3)

Scenario B now includes:

"max_trades_per_symbol": 1,
"daily_max_loss": 1000.0


A new diagnostic:

Error: fallback used


alerts you when JSON is incomplete, preventing silent misconfiguration.

After updating scenarios.json, no fallback warnings occur.

3.4 Cleanup

Deprecated configuration paths removed:

config/config_models.py → UNUSED

config/topgappers.json → UNUSED

scripts/Get-TopGappers.ps1 → UNUSED

The active config pipeline is now:

scenarios.json → config_models → cli.py → backtester.py → strategy.py

4. STATE OF THE SYSTEM AT START OF v0.8.1.0.0
4.1 Scenario B is now fully correct and JSON-driven

Scanner, Strategy, and Risk all pull from Scenario B’s params.

4.2 No fallback errors

The error diagnostic system is functioning and silent under correct configuration.

4.3 The backtester behavior is unchanged

Same signals

Same trades

Same PnL

Same child-friendly summaries

This proves config unification did not alter strategy logic.

4.4 The system is now clean and ready for the next architectural upgrade

TWCS (Trade-Window Candle Snapshot System)
The snapshot system requires a fully stable configuration pipeline before being added. v0.7.9.7.6 achieved this.

5. LOCAL WEBSITE STATUS (React, Claude) — STABLE

The local UI currently supports:

Date selection

Scenario selection

Top-N

Price filters

MACD toggle and rise bars

Additional knobs (currently placeholders)

Workflow:

All UI changes come from Claude

Claude modifies .tsx files under your instructions

The UI patches scenarios.json via the patch server

The UI is ready for TWCS integration (e.g., snapshot settings, preview indicators) but no UI work is included in v0.8.1.0.0 unless you request it.

6. WHAT THE NEXT VERSION (v0.8.1.0.0) IS ABOUT
Version Goal:

Implement the TWCS (Trade-Window Candle Snapshot System), Phase 1:

Pre-entry and post-exit candle data extraction

1-minute and 1-second bar windows

PNG snapshot generation

Metadata JSON generation

Polygon access handled exactly as in topgappers.py:

Load .env

Strip quotes

Bearer token

No apiKey URL parameter

Important:

The relational database will NOT be added in v0.8.1.0.0.
It will be introduced in a later version, only after TWCS snapshot data exists.

7. NEXT VERSION FILES EXPECTED TO BE MODIFIED

(Not to be modified manually — Copilot only.)

backtester.py — trade lifecycle hooks

A new module under snapshots/ (e.g. twcs.py or candle_snapshot.py)

A new Polygon data loader for TWCS

config_models.py — to add TWCS enable/disable flag

scenarios.json — controlled Copilot addition of TWCS flags

8. GIT COMMANDS (Finalizing v0.7.9.7.6 before starting v0.8.1.0.0)

Use these once v0.7.9.7.6 is locked:

git add -A
git commit -m "v0.7.9.7.6: unified config (scanner/strategy/risk) + fallback diagnostic + cleaned config paths"
git tag -a v0.7.9.7.6 -m "v0.7.9.7.6: completed full JSON unification for Scenario B"
git push
git push --tags

End of Handover Document for v0.8.1.0.0

Paste this entire document into your next thread to restore full project context.

When you’re ready, tell me:

“I am ready for the TWCS document.”