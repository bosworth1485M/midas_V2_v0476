BEGIN COPILOT
You are editing the Midas_V2 project, starting version v0.8.1.0.4 (TWCS Phase 3 – 1-Second Windows, Part 2).

Goal of this small step:
- Wire the existing 1-second TWCS loader into the backtester’s TWCS snapshot blocks.
- Ensure that entry, TP exit, and SL exit snapshots now include 1-second candles (`candles_1s`) and metadata.
- Do NOT change any trading logic, indicators, or strategy behavior.
- Add inline comments that clearly mark v0.8.1.0.4 near any new/modified code.

Important:
- In v0.8.1.0.3 we created a standalone loader:
  - File: src/midas_v2/dataio/twcs_second_loader.py
  - Function: load_twcs_second_window(...)
- In this version we ONLY modify: src/midas_v2/engine/backtester.py
- All other files must remain unchanged.

File to edit:
src/midas_v2/engine/backtester.py

Context:
- This file already:
  - Runs the main backtest loop.
  - Builds TWCS snapshots for:
    - Entry TWCS
    - TP (take-profit) exit TWCS
    - SL (stop-loss) exit TWCS
  - Each snapshot currently contains, at minimum:
    - 1-minute candles (`candles_1m`) loaded via the minute TWCS loader.
    - Indicator dicts (added in v0.8.1.0.2).
- We now want to attach a 1-second window to each of these TWCS snapshots using the existing loader.

Your task is to:
1) Import the loader at the top of backtester.py.
2) In the entry TWCS block, call the loader and add `candles_1s` + metadata into the snapshot.
3) Repeat the same pattern for TP and SL TWCS blocks.
4) Add clear inline comments including the version number v0.8.1.0.4 near each new/modified block.

Details:

1) Add import at the top of backtester.py

Find the import section where other midas_v2 modules are imported (e.g. indicators, TWCS minute loader, etc.), and add:

    # v0.8.1.0.4: 1-second TWCS window loader
    from midas_v2.dataio.twcs_second_loader import load_twcs_second_window

Keep this grouped with other midas_v2 imports.

2) Entry TWCS snapshot: wire in 1-second candles

Locate the code that builds the **entry TWCS snapshot**. It will be near where we:
- Have already loaded 1-minute TWCS candles (e.g. via some twcs_minute_loader).
- Build a dict representing the entry snapshot.
- Attach indicators (from v0.8.1.0.2).

Within that entry snapshot block, do the following:

- After candles_1m have been loaded and before the final snapshot dict is completed, call `load_twcs_second_window`:

    # v0.8.1.0.4: load 1-second TWCS window around entry time
    entry_candles_1s, entry_meta_1s = load_twcs_second_window(
        symbol=sym,
        date_str=date_str,
        target_time_str=entry_time_iso,
        window_before_seconds=60,
        window_after_seconds=0,
    )

- Then, when building the entry TWCS snapshot dict (or immediately after it is created), attach the new fields:

    # v0.8.1.0.4: attach 1-second window to entry TWCS snapshot
    entry_snapshot["candles_1s"] = entry_candles_1s
    entry_snapshot["window_size_1s"] = entry_meta_1s.get("window_size_1s", 0)
    entry_snapshot["window_before_1s"] = entry_meta_1s.get("window_before_1s", 60)
    entry_snapshot["window_after_1s"] = entry_meta_1s.get("window_after_1s", 0)

Notes:
- `entry_snapshot` should be the existing dict that is eventually written out to a JSON file for entry TWCS (e.g. trade_snapshot_entry_meta.json).
- If the existing code uses a different variable name (e.g. `twcs_entry`, `entry_twcs_snapshot`, etc.), use that instead.
- If you need to pick where in the dict to add these keys, add them near the existing candles/indicator fields.

3) TP exit TWCS snapshot: mirror the same pattern

Locate the code that builds the **TP (take-profit) exit TWCS snapshot**. This will be in the branch where trades exit via take-profit and we already build a TWCS snapshot for that exit (similar to entry).

Within that TP exit block:

- Call the loader using the exit time string:

    # v0.8.1.0.4: load 1-second TWCS window around TP exit time
    tp_candles_1s, tp_meta_1s = load_twcs_second_window(
        symbol=sym,
        date_str=date_str,
        target_time_str=exit_time_iso,
        window_before_seconds=60,
        window_after_seconds=0,
    )

- Attach the fields to the TP exit snapshot dict:

    # v0.8.1.0.4: attach 1-second window to TP exit TWCS snapshot
    tp_snapshot["candles_1s"] = tp_candles_1s
    tp_snapshot["window_size_1s"] = tp_meta_1s.get("window_size_1s", 0)
    tp_snapshot["window_before_1s"] = tp_meta_1s.get("window_before_1s", 60)
    tp_snapshot["window_after_1s"] = tp_meta_1s.get("window_after_1s", 0)

Again, use the actual snapshot variable name that already exists in the file (for example `twcs_exit_tp_snapshot`, `tp_twcs_snapshot`, etc.).

4) SL exit TWCS snapshot: same pattern as TP

Find the code path where a trade exits via stop-loss and we build an SL TWCS snapshot.

Within that SL block:

- Call the loader with the SL exit time (often also `exit_time_iso` in that path; if there is a separate time variable, use the one that matches the SL exit):

    # v0.8.1.0.4: load 1-second TWCS window around SL exit time
    sl_candles_1s, sl_meta_1s = load_twcs_second_window(
        symbol=sym,
        date_str=date_str,
        target_time_str=exit_time_iso,
        window_before_seconds=60,
        window_after_seconds=0,
    )

- Attach the fields to the SL exit snapshot dict:

    # v0.8.1.0.4: attach 1-second window to SL exit TWCS snapshot
    sl_snapshot["candles_1s"] = sl_candles_1s
    sl_snapshot["window_size_1s"] = sl_meta_1s.get("window_size_1s", 0)
    sl_snapshot["window_before_1s"] = sl_meta_1s.get("window_before_1s", 60)
    sl_snapshot["window_after_1s"] = sl_meta_1s.get("window_after_1s", 0)

As before, use the correct variable name for the SL TWCS snapshot.

5) Error handling expectations

- You do NOT need to add any extra try/except here because the loader itself is already defensive and never raises (it logs warnings and returns empty data on failure).
- It is acceptable and expected that, until 1-second CSVs exist, `candles_1s` will often be an empty list and `window_size_1s` will be 0.

6) Inline comments with version tag

For all new or modified code blocks, include a short inline comment containing the version number, for example:

    # v0.8.1.0.4: 1-second TWCS window loader
    ...
    # v0.8.1.0.4: attach 1-second window to entry TWCS snapshot

This is important for traceability in this project.

7) Do NOT:

- Do NOT modify any trading decision logic (entries, exits, position sizing).
- Do NOT modify any indicators or TWCS indicator computations.
- Do NOT change the names or behavior of existing functions.
- Do NOT touch any scripts, DB code, or UI code.
- Do NOT add PNG rendering or plotting here (that will be done in a later version).

At the end of this change:
- src/midas_v2/engine/backtester.py imports load_twcs_second_window.
- Entry, TP exit, and SL exit TWCS snapshots have additional fields:
    - "candles_1s"
    - "window_size_1s"
    - "window_before_1s"
    - "window_after_1s"
- No other files are modified.

Please output ONLY the updated contents of src/midas_v2/engine/backtester.py with the changes described above applied.
END COPILOT