# Midas_V2 Checkpoint – v0.3.17 (Catalyst-RVOL)

**Date:** 2025-09-16  
**Tag:** v0.3.17-catalyst-rvol

---

## What Was Done (already in tag)
- **config/scenarios.json** cleaned → only A–E remain, backups removed, D set to SL=2.5.
- **Docs/refresh_docs.py** patched → always syncs DEV_GUIDE scenario tables from scenarios.json.
- **Docs updated**: DEV_GUIDE.md, USER_GUIDE.md, SCRIPTS_INDEX.md all refreshed and committed.

This means the documentation and config are finally aligned with the live scenarios.

---

## What Was *Not* Done Yet
- Catalyst workflow implementation is **not complete yet**:
  - Top-N catalyst universes (A backbone, B filler).
  - Denylist filtering (e.g. Chinese ADRs, OTC tickers).
  - Writing `data/universe_active.txt` after enrichment + denylist.
  - Opening RVOL gate (≥1.5 in first 10–15 minutes).
  - Validation runs for D, B, E across August and September.

---

## Next Actions
1. Seed **denylist** (`data/denylist.txt`).
2. Run `scripts/enrich_universe_catalyst.py` for a test day, confirm Top-N + denylist.
3. Ensure `data/universe_active.txt` is produced (add wrapper/helper if needed).
4. Enable RVOL gate filter.
5. Run scenarios D, B, E for August → summarize results.
6. Repeat for September.

---

## Notes
- v0.3.17 tag name ("catalyst-rvol") reflects the milestone *goal*, but current commit is the **setup stage**: clean scenarios + doc sync.  
- Actual catalyst + RVOL logic is the next focus step.  

---

*Prepared as checkpoint summary for clarity when resuming work.*
