COPILOT SPEC (FINAL — LOCKED)
Midas_V2 v0.8.1.9.0
Feature: Marginal-Day Participation (Allow Exactly 1 Trade When close_gt_vwap_cnt == 1)

IMPORTANT:
- You are NOT to run any scripts or commands.
- You are ONLY to edit code.
- Output must be code changes only.

GOAL (EXACT)
On marginal days where require_day_follow_through == True and close_gt_vwap_cnt == 1 (computed by existing DAY_GATE),
allow exactly ONE Scenario B trade for the entire day (the first valid entry signal).
After that one trade closes (TP/SL/ERR), block all further entries for the rest of the day.
Hostile days (cnt==0) and healthy days (cnt>=2) must behave exactly as before.

FILES ALLOWED TO CHANGE (ONLY)
- src/midas_v2/engine/backtester.py

ABSOLUTE RULES
- Do NOT change strategy.py or strat.should_enter logic.
- Do NOT change entries/exits/TP/SL/sizing/RiskManager/DAY_GATE logic.
- Do NOT add config keys.
- Do NOT add new modules or helpers.
- Keep changes minimal and deterministic.
- Add inline comments with version v0.8.1.9.0 near new/changed lines.

IMPLEMENTATION REQUIREMENTS
1) Define marginal-day flag using existing variables:
   is_marginal_day = (require_day_follow_through and close_gt_vwap_count == 1)

2) Add a day-level counter:
   day_trade_count = 0
   Initialize once per day near other guardrail trackers.

3) Increment day_trade_count exactly once per completed trade close (TP/SL/ERR_*),
   at the same place where trades_by_symbol[sym] and cum_pnl are updated.
   Ensure BOTH TP and SL close paths increment.

4) Enforce marginal-day cap:
   Before the existing entry condition, add a guard:
   if is_marginal_day and day_trade_count >= 1:
       block new entries (continue)
   This must not affect hostile or healthy days.

5) Logging:
   - After the existing DAY_GATE_SUMMARY log, add one line:
     [WHY] v0.8.1.9.0 MARGINAL_DAY_POLICY details=is_marginal_day=<bool> marginal_trade_cap=1
   - When blocking due to cap, emit a log-once line:
     [WHY] v0.8.1.9.0 EARLY_REJECT reason=MARGINAL_DAY_CAP symbol=<sym> ts=<bar.ts> details=day_trade_count=1 cap=1
   Reuse existing early_reject_logged set to avoid spam.

INSERTION POINTS (CRITICAL)
- Define is_marginal_day after DAY_GATE prepass and after STRUCT_DAMAGE auto-switch is finalized,
  adjacent to DAY_GATE_SUMMARY, before per-symbol loop.
- Enforce the cap in the per-bar loop immediately before the existing combined entry condition
  (the one that checks day_gate_failed/position/pending_entry/strat.should_enter/risk.allow_new_trade).
- Increment day_trade_count at trade close for both TP and SL paths near where trades_by_symbol and cum_pnl are updated.

DELIVERABLE
- One small, surgical commit
- backtester.py only
- No behavior changes outside marginal-day cap

END SPEC
