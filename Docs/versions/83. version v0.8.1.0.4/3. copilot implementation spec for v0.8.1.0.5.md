BEGIN COPILOT SPEC FOR v0.8.1.0.5

You are modifying the Midas_V2 project, now at version v0.8.1.0.5.
Goal: Implement TWCS PNG rendering (TWCS Phase 3 – Part 3).

IMPORTANT:
- Do NOT modify trading logic, indicators, sizing, filters, risk management, or strategy flow.
- Only add visualization capabilities.
- All new code must include inline version tags:  # v0.8.1.0.5
- You will create ONLY TWO changes:
    (1) A new module: src/midas_v2/plotting/twcs_plotter.py
    (2) A small integration hook inside backtester.py (behind a flag)

------------------------------------------------------------
1. CREATE NEW FILE: src/midas_v2/plotting/twcs_plotter.py
------------------------------------------------------------

In this file, implement a TWCS rendering helper with:

    import matplotlib
    matplotlib.use("Agg")  # v0.8.1.0.5: headless mode

    import matplotlib.pyplot as plt

    def plot_twcs_snapshot(snapshot: dict, out_path: str) -> None:
        """
        v0.8.1.0.5
        Render a 1-minute + 1-second candle visualization.
        Snapshot dict contains:
            candles_1m: list of dicts (t, o, h, l, c, v, idx_from_entry)
            candles_1s: list of dicts (same fields)
            entry_time or exit_time
            symbol
        Saves PNG to out_path.
        """

Requirements:
- Two vertically stacked subplots:
    - Top: 1-minute candles
    - Bottom: 1-second candles
- Use standard OHLC plotting:
    - Green if close >= open
    - Red if close < open
- Draw vertical line at trade time (entry or exit)
- Add labels:
    - Title: f"{snapshot['symbol']} – Entry TWCS" or "Exit TWCS"
- Save via plt.savefig(out_path, dpi=140)
- Ensure plt.close() is called to avoid memory leaks

------------------------------------------------------------
2. MODIFY BACKTESTER.PY (SMALL CHANGE)
------------------------------------------------------------

Inside src/midas_v2/engine/backtester.py:
- Add import at top with version tag:

    from midas_v2.plotting.twcs_plotter import plot_twcs_snapshot  # v0.8.1.0.5

- Inside the code path where entry TWCS snapshot JSON is written:

    if params.plot_twcs:   # v0.8.1.0.5
        out_png = os.path.join(snapshot_dir, "trade_snapshot_entry.png")
        plot_twcs_snapshot(entry_snapshot, out_png)  # v0.8.1.0.5

- Inside the code path where exit TWCS snapshot JSON is written:

    if params.plot_twcs:   # v0.8.1.0.5
        out_png = os.path.join(snapshot_dir, "trade_snapshot_exit.png")
        plot_twcs_snapshot(exit_snapshot, out_png)  # v0.8.1.0.5

------------------------------------------------------------
3. CONFIG FLAG
------------------------------------------------------------

Add a new scenario parameter:
    plot_twcs: bool = False

Ensure backtester reads this parameter from scenarios.json.

------------------------------------------------------------
4. FINAL OUTPUT REQUIREMENT
------------------------------------------------------------

At the end of your response, Copilot MUST output only:

The full contents of:
    - src/midas_v2/plotting/twcs_plotter.py
    - the updated src/midas_v2/engine/backtester.py

END COPILOT SPEC FOR v0.8.1.0.5
