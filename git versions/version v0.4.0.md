git status -u

git add -A

git commit -m "v0.4.0: Adaptive sizing baseline (neutral C, A on RVOL>=2.6, cap=50) + tools"

git tag -a v0.4.0 -m "Adaptive sizing baseline verified on Aug-05..07"

git push

git push --tags

