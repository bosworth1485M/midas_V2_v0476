git add -A; git commit -m "v0.3.46: Scenario B profit-guard (score>=3 + Top-2 + RVOL>=2.4 + gate=20) + Bearer auth + boosted scoring + viewers ok"

git tag -a v0.3.46 -m "Scenario B profit-guard baseline"

git push; git push --tags

